# For self update:
WF_PROGRAM_REPOSITORY=git@gitlab.webtown.hu:pub/wf.git

# Directory structure
WF_WORKING_DIRECTORY_NAME=.wf
WF_CONFIGURATION_FILE_NAME=.wf.yml
WF_ENV_FILE_NAME=.wf.env

# Symfony . You can use the --dev argument to change it once through run!
WF_SYMFONY_ENV=prod
WF_XDEBUG_ENABLED=0

# Default DNS local proxy TLD:
WF_DEFAULT_LOCAL_TLD=.loc

# The program try to use the system value. If you want to override the system values, you can override it here.
#WF_HOST_TIMEZONE=Europe/Budapest
#WF_HOST_LOCALE=hu_HU.UTF-8
